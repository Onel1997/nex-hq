/**
 * Milaene Brain — shared knowledge interface.
 *
 * Implementation deferred to Phase 1 (Supabase schema + read/write handlers).
 * Agents must use this module rather than querying storage directly.
 */

export type {
  BrainEntry,
  BrainEntryStatus,
  BrainEntryType,
  BrainQuery,
  BrainWriteInput,
} from "./types";
