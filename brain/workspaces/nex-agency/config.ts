import type { WorkspaceDefinition } from "../types";

/** NexAgency — creative agency workspace (seed data pending). */
export const NEXAGENCY_WORKSPACE: WorkspaceDefinition = {
  slug: "nex-agency",
  name: "NexAgency",
  industryId: "agency_hq",
  seedRecords: [],
};
