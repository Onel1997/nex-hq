import type { WorkspaceDefinition } from "../types";

/** NexTrends — trend intelligence workspace (seed data pending). */
export const NEXTRENDS_WORKSPACE: WorkspaceDefinition = {
  slug: "nex-trends",
  name: "NexTrends",
  industryId: "creator_hq",
  seedRecords: [],
};
