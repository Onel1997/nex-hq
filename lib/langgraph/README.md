# LangGraph — Future Agent Orchestration

`@langchain/langgraph` is installed and ready for Phase 3.

## Planned Usage

- CEO Agent graph: goal intake → task decomposition → specialist routing → synthesis
- Specialist subgraphs: plan → execute → validate → report
- Checkpointed state for long-running workflows

## Not Implemented

No graphs, nodes, or edges are defined yet. See `docs/agents.md` and `docs/roadmap.md`.
