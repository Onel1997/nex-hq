# Face-api model weights for LocalFaceEmbeddingEvaluator
#
# Populated by: npm run copy:face-api-models (also runs on postinstall)
# Source: node_modules/@vladmandic/face-api/model
#
# These are public ML weight files — not biometric data.
# Candidate images and face embeddings remain server-only / private.
