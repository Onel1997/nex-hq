/**
 * Task system — work units delegated by CEO or created by humans.
 * Handlers and persistence deferred to Phase 2.
 */

export type {
  CreateTaskInput,
  Task,
  TaskPriority,
  TaskStatus,
} from "./types";
